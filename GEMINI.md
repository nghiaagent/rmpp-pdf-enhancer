# Project rules

## Version control & pull request workflow

- **Branches and pull requests only**: Always create a feature branch and open a pull request (PR) for any changes.
- **Do not merge**: Never merge pull requests or push directly to `main`. Leave PR review and merging to the user.
- **GitButler (`but`)**: Use `but` for all branch and commit operations.
